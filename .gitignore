# ============================================================
#  Dependencies
# ============================================================
node_modules/
.pnp/
.pnp.js
.pnp.cjs
.pnp.data.json
.yarn/

# pnpm / corepack workarounds
pnpm-debug.log*
corepack.*
corepack/

# ============================================================
#  Build output / TypeScript / Vite artifacts
# ============================================================
dist/
dist-ssr/
build/
out/
*.local
*.tsbuildinfo
*.tsbuildinfo.*
node_modules/.tmp/
.vite/
.vite-temp/
.cache/
.temp/
.tmp/

# vue-tsc incremental
*.vue-tsc.json

# ============================================================
#  Environment / secrets
# ============================================================
.env
.env.*
!.env.example
.env.local.*

# ============================================================
#  Logs
# ============================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ============================================================
#  IDE / Editor
# ============================================================
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~
.project
.classpath
.settings/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ============================================================
#  OS
# ============================================================
.DS_Store
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/

# ============================================================
#  Project-local development scratch / debug artifacts
# （日历开发、Design Lab 调试时可能随手生成的本地文件）
# ============================================================
.superpowers/
exp.jpg
*.screenshot.png
screenshot*.png
debug/
tmp/
scratch/
_/
# 历史上曾经出现过的：把输出重定向到本地文件 `npm` 时产生的 0-byte 占位
npm
# Auth / browser automation state files dropped locally
*.agent-browser-state.json
agent-browser-state/
browser-state/
cookies.json
localStorage.json

# ============================================================
#  用户本地个人素材 / 运行时数据（绝不进入 GitHub）
#  ------------------------------------------------------------
#  · 项目所有真正的用户数据（事件、固定背景、Dashboard 图片组件）都存于
#    浏览器 IndexedDB（Dexie，库名 mymemo）；Design Lab Token、日历开关、
#    固定事件、Widget 布局存于浏览器 localStorage。这些不落在项目文件里，
#    因此 Clone 得到的仓库天然是"干净的空 MYMEMO"，无需额外清理。
#  · 下列图片是唯一的、留在项目目录里的个人素材（非源码、非运行时依赖）：
#      bg.jpg          —— 本地壁纸原图（运行时壁纸以 base64 存 IndexedDB，不依赖此文件）
#      icon_orgin.jpg  —— 设计参考原图（运行时 logo/favicon 用 public/favicon.png，
#                          此图仅是个人取材来源，代码只在注释里引用）
#    二者已用 `git rm --cached` 解除 Git 跟踪（本地文件保留）。
# ============================================================
bg.jpg
icon_orgin.jpg

# ============================================================
#  Snapshot / preview
# ============================================================
coverage/
*.lcov
.vitest/
playwright-report/
test-results/
storybook-static/
启动开发环境.bat
PROJECT_STATUS.md
/.workbuddy
